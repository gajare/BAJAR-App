version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: product_catalog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  github.com/gajare/BAJAR-App/Backend/Product-Catalog-Service/Product:
    build: .
    ports:
      - "8082:8082"
    environment:
      DATABASE_URL: "host=postgres user=postgres password=postgres dbname=product_catalog port=5432 sslmode=disable"
      PORT: "8082"
    depends_on:
      - postgres

volumes:
  postgres_data: